pub mod column;
pub mod data;
/**
 * Record Management Module
 * consists of three conponents:
 * + Database (metadata) records
 * + Table (metadata) records
 * + Data entry records
 */
pub mod database;
pub mod table;
